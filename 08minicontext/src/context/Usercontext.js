import React from "react";

const Usercontext=React.createContext()

export default Usercontext

//with context you need to make a provider 
//wrapping of all the components into a single global one=provider